
const User = {
    find: async function (input) {
        //filter users as per given filters
        //Its great if we use index on filtered columns, that will speed up the operation
    }
    add: async function (input) {
        //Add user in db
    }

}
module.exports = User;